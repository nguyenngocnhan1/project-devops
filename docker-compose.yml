version: "3.8"

services:
  backend:
    build: ./backend
    container_name: backend
    ports:
      - "4000:4000"
    restart: always

  frontend:
    build: ./frontend
    container_name: frontend
    ports:
      - "80:80"
    restart: always
